[pytest]
pythonpath = backend .
testpaths = tests
markers =
    docker: needs a reachable Docker daemon and built sandbox images
    e2e: needs the full stack running (docker compose up); set CODEBOX_E2E_URL
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning:passlib.*
